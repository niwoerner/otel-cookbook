# Nginx Setup

To ensure this nginx setup works properly, place the files in the correct directory relative to your docker-compose files.
Eensure to follow the path of the [docker-compose-file](../docker-compose.yml).
